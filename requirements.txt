# Chrome's TLS fingerprint over plain HTTP -- this is what gets past Cloudflare.
curl_cffi>=0.7
PyYAML>=6.0
requests>=2.31

# Optional: only needed for the `engine: browser` fallback.
#   pip install playwright && python -m playwright install chromium
# playwright>=1.45
