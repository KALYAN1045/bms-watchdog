services:
  watchdog:
    build: .
    image: bms-watchdog
    restart: unless-stopped
    env_file: .env
    volumes:
      - .:/app               # watchlist.yaml and state.json live on the host
    stop_grace_period: 30s
